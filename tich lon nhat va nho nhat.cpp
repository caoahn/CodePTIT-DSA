#include<bits/stdc++.h> 
using namespace std ;  

int main() { 
    int t ; 
    cin >> t ; 
    while(t--) { 
       int 	n,m ; 
       cin >> n >> m ; 
       int a[n] ; 
       int b[m];
       for(int &x : a) cin >> x ; 
       for(int &x : b) cin >> x; 
       vector<int> v1 ;
	   vector<int> v2;  
	    
       cout << a[n-1] * b[0] << endl ; 
	}
}
